# RE:carrot 작업 지침

작업 전에 README.md와 docs/PROJECT_CONTEXT.md를 읽는다. AI OS Wiki가 연결되어 있으면 `📗 RE-carrot 현재 구현 및 인계 기준`을 가장 먼저 읽는다. 다른 컴퓨터에서 이어지는 작업의 공통 컨텍스트는 저장소의 이 문서다. 기존 로컬 Obsidian 위키가 연결되어 있다면 최신 Active Context·Runbook·Session Brief·Decision Log도 참고하되, 위키가 없는 컴퓨터에서는 저장소 문서를 기준으로 진행한다. 과거 컴퓨터의 절대 경로를 필수 의존성으로 만들지 않는다.

- 분석 대상은 대한민국 당근 모바일 앱이다. 웹 기술은 프로토타입 실행 수단이다.
- 캡처를 먼저 재현한 뒤 승인된 기획에 따라 수정한다. 홈부터 화면 하나씩 진행한다.
- SEED를 우선 사용하되 캡처와 맞지 않으면 직접 구현한다.
- 이미지 작업 전에 public/reference의 실제 파일 목록을 확인하고 관련 이미지를 직접 연다. 원본이 다른 작업 폴더에 추가되었다면 해당 폴더도 확인한다. 파일명으로 내용을 단정하거나 파일 확인 없이 재업로드를 요구하지 않는다.
- 목록과 상세의 판매자 위치는 공통 sellers 데이터에서 읽는다. 박경덕은 서울특별시, 유주연은 경상북도로 인증지역·거래 장소·수거지를 고정한다(2026-09-16 최신 사용자 변경). 보조 동네는 없다. 실제 인증은 구현하지 않는다.
- 바로구매 상품은 현재 계정과 판매자의 시·도 ID가 같으면 채팅하기, 다르면 질문하기다. 이는 사용자 승인 프로토타입 가정이며 실제 당근 정책이라고 설명하지 않는다.
- 직접 관찰, 사용자 설명, 공식 근거, 기획 해석을 구분한다.
- 기획 범위는 일반 이용자의 바로구매 상세부터 결제 직전까지다. 커뮤니티·예약·좌석예매 및 그 목적지, 광고주 화면·광고 집행 개선은 제외한다. 캡처의 외형 재현과 후속 기능 구현을 구분한다.
- 기존 채팅없이 질문하기와 배송비 계산을 신규 기능으로 제안하지 않는다. 기각된 검수 구매·판매 대행·멤버십 후보를 다시 제안하지 않는다.
- 사용자의 기존 변경을 보존한다. 작업 시작 때 git 상태를 확인하고, 변경에 적합한 검증을 수행한 뒤 결과와 한계를 보고한다.
- 의미 있는 구현·결정·검증·다음 작업은 docs/PROJECT_CONTEXT.md에 갱신한다. 연결된 위키가 있으면 장기 기록도 동기화한다.
- 다음 화면이나 미제공 사진을 임의로 만들어 범위를 확장하지 않는다. 상세 채팅·결제 흐름의 다음 구현은 사용자의 요청과 제공 자료에 맞춘다.

프로젝트 표시명은 RE:carrot이며 파일·폴더명에는 RE-carrot을 사용한다.
